{
  "name": "@tokenring-ai/browser-file-system",
  "description": "Provides a browser-based mock FileSystemService using in-memory data.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "workspace:*",
    "@tokenring-ai/filesystem": "workspace:*",
    "@tokenring-ai/utility": "workspace:*"
  },
  "license": "MIT",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "eslint": "eslint --fix ./",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  }
}
